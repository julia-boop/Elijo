<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %>
    <body>
        <%- include('./partials/header') %>
        <div class="container">

            <% if(typeof success != 'undefined'){ %>
                <% if(success){ %>
                    <div class="alert alert-success" role="alert">
                        Muchas gracias por aportar en ELIJO!
                    </div>
                <% }else{ %>
                    <div class="alert alert-danger" role="alert">
                        Opss! Hubo un error... Vuelve a intentarlo mas tarde
                    </div>
                <% } %>
            <% } %>

            <h4 class="statInfo">A través de este formulario podes dejar una calificación sobre las distintas instituciones académicas que transitaste para que podamos construir en base a las opiniones estudiantiles, una fuente de información confiable para futuros estudiantes...</h4>

            <form class="mt-5 d-flex flex-column justify-content-center form-stat" action="/user/createStats/<%=user.id%>" method="POST">
                <div class="to-add" id="stat">
                    <h5>¿Que queres calificar?</h5>
                    <select name="toAddStats" required>
                        <% for(let i = 0; i < user.User_careers.length; i++){ %>
                            <option value="<%= user.User_careers[i].id %>,Career"><%= user.User_careers[i].name %></option>
                        <% } %>
                        <% for(let j = 0; j < user.User_courses.length; j++){ %>
                            <option value="<%= user.User_courses[j].id %>,Course"><%= user.User_courses[j].name %></option>
                        <% } %>
                    </select>
                </div>
                
                <div class="difficulty" id="stat">
                    <h5 class="mt-3">Dificultad</h5>
                    <select name="difficulty" id="difficulty">
                        <% for(let i = 0; i <= 10; i++){ %>
                            <option value="<%= i %>"><%= i %></option>
                        <% } %>
                    </select>
                </div>

                <div class="job" id="stat">
                    <h5 class="mt-3">Salida Laboral</h5>
                    <select name="job_exit" id="job_exit">
                        <% for(let i = 0; i <= 10; i++){ %>
                            <option value="<%= i %>"><%= i %></option>
                        <% } %>
                    </select>
                </div>

                <div class="study" id="stat">
                    <h5 class="mt-3">Horas de estudio</h5>
                    <select name="study_hours" id="study_hours">
                        <% for(let i = 0; i <= 10; i++){ %>
                            <option value="<%= i %>"><%= i %></option>
                        <% } %>
                    </select>
                </div>

                <div class="btn-container d-flex justify-content-center">
                    <button type="submit" class="stat-btn-submit">Enviar datos</button>
                </div>
            </form>
            
        </div>
        
        
        <%- include('./partials/scripts.ejs') %>
        <%- include('./partials/footer') %>  
    </body>
</html>