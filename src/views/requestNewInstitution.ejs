<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %>
    <body>
        <%- include('./partials/header') %> 
        <div class="container register-container">
            

            <form class="text-center border border-light p-5" action="" method="POST">
                <% if(typeof success != 'undefined'){ %>
                    <div class="alert alert-success" role="alert">
                        Muchas Gracias! Estamos trabajando para mejorar.
                    </div>
                <% } %>
                

                <p class="h4 mb-4 register-title">Ayudanos a mejorar</p>
    
                <div class="form-row mb-4">
                    <div class="col">
                        <!-- First name -->
                        <input name="user_name" type="text" id="defaultRegisterFormFirstName" class="form-control" placeholder="Nombre" value="<%=(typeof user != 'undefined' && user.name != null)? user.name : '' %>">
                    </div>
                    <div class="col">
                        <!-- Last name -->
                        <input name="user_last_name" type="text" id="defaultRegisterFormLastName" class="form-control" placeholder="Apellido" value="<%=(typeof user != 'undefined' && user.last_name != null)? user.last_name : '' %>">
                    </div>
                </div>
    
                <!-- E-mail -->
                <input name="user_email" type="email" id="defaultRegisterFormEmail" class="form-control mb-4" placeholder="E-mail" value="<%=(typeof user != 'undefined' && user.email != null)? user.email : '' %>">

                <div class="d-flex justify-content-start w-100">
                    <select name="affair" id="options" class="mb-3">
                        <option value="University">Falta mi universidad</option>
                        <option value="Institute">Falta mi instituto</option>
                        <option value="Career">Falta mi carrera</option>
                        <option value="Course">Falta mi curso</option>
                    </select>
                </div>
                
                

                <textarea class="form-control" name="request" id="request" cols="30" rows="5" placeholder="¿Necesitas algo mas? Escribinos por aca!"></textarea>
                
                <button class="btn  my-4 btn-block btn-log-reg" type="submit">Enviar</button>
            </form>
        </div>
        
        <%- include('./partials/footer') %>
        <%- include('./partials/scripts') %>
    </body>
</html>